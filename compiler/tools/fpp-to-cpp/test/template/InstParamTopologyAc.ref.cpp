// ======================================================================
// \title  InstParamTopologyAc.cpp
// \author Generated by fpp-to-cpp
// \brief  cpp file for InstParam topology
// ======================================================================

#include "InstParamTopologyAc.hpp"

// ----------------------------------------------------------------------
// Component instances
// ----------------------------------------------------------------------

CIn cIn(FW_OPTIONAL_NAME("cIn"));

COut cOut(FW_OPTIONAL_NAME("cOut"));

namespace M {

  // ----------------------------------------------------------------------
  // Helper functions
  // ----------------------------------------------------------------------

  void initComponents(const TopologyState& state) {
    cIn.init(InstanceIds::cIn);
    cOut.init(InstanceIds::cOut);
  }

  void configComponents(const TopologyState& state) {
    // Nothing to do
  }

  void setBaseIds() {
    cOut.setIdBase(BaseIds::cOut);
    cIn.setIdBase(BaseIds::cIn);
  }

  void connectComponents() {

#if !FW_DIRECT_PORT_CALLS

    // C
    cOut.set_pOut_OutputPort(
        0,
        cIn.get_pIn_InputPort(0)
    );

#endif

  }

  void regCommands() {
    // Nothing to do
  }

  void readParameters() {
    // Nothing to do
  }

  void loadParameters() {
    // Nothing to do
  }

  void startTasks(const TopologyState& state) {
    // Nothing to do
  }

  void stopTasks(const TopologyState& state) {
    // Nothing to do
  }

  void freeThreads(const TopologyState& state) {
    // Nothing to do
  }

  void tearDownComponents(const TopologyState& state) {
    // Nothing to do
  }

  void deinitComponents(const TopologyState& state) {
    cIn.deinit();
    cOut.deinit();
  }

  // ----------------------------------------------------------------------
  // Setup and teardown functions
  // ----------------------------------------------------------------------

  void setup(const TopologyState& state) {
    initComponents(state);
    configComponents(state);
    setBaseIds();
    connectComponents();
    regCommands();
    readParameters();
    loadParameters();
    startTasks(state);
  }

  void teardown(const TopologyState& state) {
    stopTasks(state);
    freeThreads(state);
    tearDownComponents(state);
    deinitComponents(state);
  }

}

#if FW_DIRECT_PORT_CALLS

// ----------------------------------------------------------------------
// Topology-dependent component implementation
// ----------------------------------------------------------------------


bool COutComponentBase::isConnected_pOut_OutputPort(FwIndexType portNum) const {
  FW_ASSERT(
    (0 <= portNum) && (portNum < NUM_POUT_OUTPUT_PORTS),
    static_cast<FwAssertArgType>(portNum),
    static_cast<FwAssertArgType>(NUM_POUT_OUTPUT_PORTS)
  );
  bool result = false;
  switch (portNum) {
    case 0:
      result = true;
      break;
    default:
      break;
  }
  return result;
}

void COutComponentBase::pOut_out(FwIndexType portNum) const {
  FW_ASSERT(
    (0 <= portNum) && (portNum < NUM_POUT_OUTPUT_PORTS),
    static_cast<FwAssertArgType>(portNum),
    static_cast<FwAssertArgType>(NUM_POUT_OUTPUT_PORTS)
  );
  switch (portNum) {
    case 0:
      cIn.pIn_handlerBase(0);
      break;
    default:
      FW_ASSERT(false, static_cast<FwAssertArgType>(portNum));
      break;
  }
}


#endif
