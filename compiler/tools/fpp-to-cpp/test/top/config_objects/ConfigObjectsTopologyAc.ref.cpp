// ======================================================================
// \title  ConfigObjectsTopologyAc.cpp
// \author Generated by fpp-to-cpp
// \brief  cpp file for ConfigObjects topology
// ======================================================================

#include "ConfigObjectsTopologyAc.hpp"

// ----------------------------------------------------------------------
// Component instances
// ----------------------------------------------------------------------

namespace M {

  namespace Nested {

    M::C second(FW_OPTIONAL_NAME("M.Nested.second"));

  }

}

M::C emptyConfig(FW_OPTIONAL_NAME("emptyConfig"));

M::C emptyPhase(FW_OPTIONAL_NAME("emptyPhase"));

M::C first(FW_OPTIONAL_NAME("first"));

M::C noConfig(FW_OPTIONAL_NAME("noConfig"));

namespace M {

  // ----------------------------------------------------------------------
  // Component configuration objects
  // ----------------------------------------------------------------------

  namespace ConfigObjects {

    namespace M_Nested_second {
      U32 value = 22;
    }

    namespace emptyConfig {

    }

    namespace emptyPhase {
      U32 value = 55;
    }

    namespace first {
      U32 value = 11;
    }

  }

  // ----------------------------------------------------------------------
  // Helper functions
  // ----------------------------------------------------------------------

  void initComponents(const TopologyState& state) {
    {
      using namespace ConfigObjects::M_Nested_second;

      const U32 local = value;
      M::Nested::second.record(local);
    }
    emptyConfig.init(InstanceIds::emptyConfig);
    emptyPhase.init(InstanceIds::emptyPhase);
    {
      using namespace ConfigObjects::first;

      const U32 local = value;
      first.record(local);
    }
    noConfig.init(InstanceIds::noConfig);
  }

  void configComponents(const TopologyState& state) {
    {
      using namespace ConfigObjects::M_Nested_second;

      const U32 local = value;
      M::Nested::second.record(local);
    }
    {
      using namespace ConfigObjects::emptyConfig;

      emptyConfig.record(44);
    }

    {
      using namespace ConfigObjects::first;

      const U32 local = value;
      first.record(local);
    }
    noConfig.record(33);
  }

  void setBaseIds() {
    first.setIdBase(BaseIds::first);
    M::Nested::second.setIdBase(BaseIds::M_Nested_second);
    noConfig.setIdBase(BaseIds::noConfig);
    emptyConfig.setIdBase(BaseIds::emptyConfig);
    emptyPhase.setIdBase(BaseIds::emptyPhase);
  }

  void connectComponents() {
    // Nothing to do
  }

  void regCommands() {
    {
      using namespace ConfigObjects::M_Nested_second;

      const U32 local = value;
      M::Nested::second.record(local);
    }
    {
      using namespace ConfigObjects::first;

      const U32 local = value;
      first.record(local);
    }
  }

  void readParameters() {
    {
      using namespace ConfigObjects::M_Nested_second;

      const U32 local = value;
      M::Nested::second.record(local);
    }
    {
      using namespace ConfigObjects::first;

      const U32 local = value;
      first.record(local);
    }
  }

  void loadParameters() {
    {
      using namespace ConfigObjects::M_Nested_second;

      const U32 local = value;
      M::Nested::second.record(local);
    }
    {
      using namespace ConfigObjects::first;

      const U32 local = value;
      first.record(local);
    }
  }

  void startTasks(const TopologyState& state) {
    {
      using namespace ConfigObjects::M_Nested_second;

      const U32 local = value;
      M::Nested::second.record(local);
    }
    {
      using namespace ConfigObjects::first;

      const U32 local = value;
      first.record(local);
    }
  }

  void stopTasks(const TopologyState& state) {
    {
      using namespace ConfigObjects::M_Nested_second;

      const U32 local = value;
      M::Nested::second.record(local);
    }
    {
      using namespace ConfigObjects::first;

      const U32 local = value;
      first.record(local);
    }
  }

  void freeThreads(const TopologyState& state) {
    {
      using namespace ConfigObjects::M_Nested_second;

      const U32 local = value;
      M::Nested::second.record(local);
    }
    {
      using namespace ConfigObjects::first;

      const U32 local = value;
      first.record(local);
    }
  }

  void tearDownComponents(const TopologyState& state) {
    {
      using namespace ConfigObjects::M_Nested_second;

      const U32 local = value;
      M::Nested::second.record(local);
    }
    {
      using namespace ConfigObjects::first;

      const U32 local = value;
      first.record(local);
    }
  }

  void deinitComponents(const TopologyState& state) {
    {
      using namespace ConfigObjects::M_Nested_second;

      const U32 local = value;
      M::Nested::second.record(local);
    }
    emptyConfig.deinit();
    emptyPhase.deinit();
    {
      using namespace ConfigObjects::first;

      const U32 local = value;
      first.record(local);
    }
    noConfig.deinit();
  }

  // ----------------------------------------------------------------------
  // Setup and teardown functions
  // ----------------------------------------------------------------------

  void setup(const TopologyState& state) {
    initComponents(state);
    configComponents(state);
    setBaseIds();
    connectComponents();
    regCommands();
    readParameters();
    loadParameters();
    startTasks(state);
  }

  void teardown(const TopologyState& state) {
    stopTasks(state);
    freeThreads(state);
    tearDownComponents(state);
    deinitComponents(state);
  }

}
