// ======================================================================
// \title  EventsTopologyAc.cpp
// \author Generated by fpp-to-cpp
// \brief  cpp file for Events topology
// ======================================================================

#include "EventsTopologyAc.hpp"

// ----------------------------------------------------------------------
// Component instances
// ----------------------------------------------------------------------

namespace M {

  M::C c1(FW_OPTIONAL_NAME("c1"));

}

namespace M {

  M::C c2(FW_OPTIONAL_NAME("c2"));

}

namespace M {

  M::EventManager eventManager(FW_OPTIONAL_NAME("eventManager"));

}

namespace M {

  M::NoEvents noEvents(FW_OPTIONAL_NAME("noEvents"));

}

namespace M {

  // ----------------------------------------------------------------------
  // Helper functions
  // ----------------------------------------------------------------------

  void initComponents(const TopologyState& state) {
    M::c1.init(InstanceIds::M_c1);
    M::c2.init(InstanceIds::M_c2);
    M::eventManager.init(InstanceIds::M_eventManager);
    M::noEvents.init(InstanceIds::M_noEvents);
  }

  void configComponents(const TopologyState& state) {
    // Nothing to do
  }

  void setBaseIds() {
    M::c1.setIdBase(BaseIds::M_c1);
    M::c2.setIdBase(BaseIds::M_c2);
    M::eventManager.setIdBase(BaseIds::M_eventManager);
    M::noEvents.setIdBase(BaseIds::M_noEvents);
  }

  void connectComponents() {

#if !FW_DIRECT_PORT_CALLS

    // Events
    M::c1.set_eventOut_OutputPort(
        0,
        M::eventManager.get_eventIn_InputPort(0)
    );

#endif

  }

  void regCommands() {
    // Nothing to do
  }

  void readParameters() {
    // Nothing to do
  }

  void loadParameters() {
    // Nothing to do
  }

  void startTasks(const TopologyState& state) {
    // Nothing to do
  }

  void stopTasks(const TopologyState& state) {
    // Nothing to do
  }

  void freeThreads(const TopologyState& state) {
    // Nothing to do
  }

  void tearDownComponents(const TopologyState& state) {
    // Nothing to do
  }

  // ----------------------------------------------------------------------
  // Setup and teardown functions
  // ----------------------------------------------------------------------

  void setup(const TopologyState& state) {
    initComponents(state);
    configComponents(state);
    setBaseIds();
    connectComponents();
    regCommands();
    readParameters();
    loadParameters();
    startTasks(state);
  }

  void teardown(const TopologyState& state) {
    stopTasks(state);
    freeThreads(state);
    tearDownComponents(state);
  }

}

#if FW_DIRECT_PORT_CALLS

// ----------------------------------------------------------------------
// Topology-dependent component implementation
// ----------------------------------------------------------------------

namespace M {

  bool CComponentBase::isConnected_eventOut_OutputPort(FwIndexType portNum) const {
    FW_ASSERT(
      (0 <= portNum) && (portNum < NUM_EVENTOUT_OUTPUT_PORTS),
      static_cast<FwAssertArgType>(portNum),
      static_cast<FwAssertArgType>(NUM_EVENTOUT_OUTPUT_PORTS)
    );
    bool result = false;
    const auto instance = this->getInstance();
    switch (instance) {
      case ::M::InstanceIds::M_c1:
        switch (portNum) {
          case 0:
            result = true;
            break;
          default:
            break;
        }
        break;
      default:
        FW_ASSERT(0, static_cast<FwAssertArgType>(instance));
        break;
    }
    return result;
  }

#if FW_ENABLE_TEXT_LOGGING == 1

  bool CComponentBase::isConnected_textEventOut_OutputPort(FwIndexType portNum) const {
    FW_ASSERT(
      (0 <= portNum) && (portNum < NUM_TEXTEVENTOUT_OUTPUT_PORTS),
      static_cast<FwAssertArgType>(portNum),
      static_cast<FwAssertArgType>(NUM_TEXTEVENTOUT_OUTPUT_PORTS)
    );
    bool result = false;
    const auto instance = this->getInstance();
    switch (instance) {
      default:
        FW_ASSERT(0, static_cast<FwAssertArgType>(instance));
        break;
    }
    return result;
  }

#endif

  bool CComponentBase::isConnected_timeGetOut_OutputPort(FwIndexType portNum) const {
    FW_ASSERT(
      (0 <= portNum) && (portNum < NUM_TIMEGETOUT_OUTPUT_PORTS),
      static_cast<FwAssertArgType>(portNum),
      static_cast<FwAssertArgType>(NUM_TIMEGETOUT_OUTPUT_PORTS)
    );
    bool result = false;
    const auto instance = this->getInstance();
    switch (instance) {
      default:
        FW_ASSERT(0, static_cast<FwAssertArgType>(instance));
        break;
    }
    return result;
  }

  void CComponentBase::eventOut_out(
      FwIndexType portNum,
      FwEventIdType id,
      Fw::Time& timeTag,
      const Fw::LogSeverity& severity,
      Fw::LogBuffer& args
  ) const {
    FW_ASSERT(
      (0 <= portNum) && (portNum < NUM_EVENTOUT_OUTPUT_PORTS),
      static_cast<FwAssertArgType>(portNum),
      static_cast<FwAssertArgType>(NUM_EVENTOUT_OUTPUT_PORTS)
    );
    const auto instance = this->getInstance();
    switch (instance) {
      case ::M::InstanceIds::M_c1:
        switch (portNum) {
          case 0:
            M::eventManager.eventIn_handlerBase(
              0,
              id,
              timeTag,
              severity,
              args
            );
            break;
          default:
            FW_ASSERT(0, static_cast<FwAssertArgType>(portNum));
            break;
        }
        break;
      default:
        FW_ASSERT(0, static_cast<FwAssertArgType>(instance));
        break;
    }
  }

#if FW_ENABLE_TEXT_LOGGING == 1

  void CComponentBase::textEventOut_out(
      FwIndexType portNum,
      FwEventIdType id,
      Fw::Time& timeTag,
      const Fw::LogSeverity& severity,
      Fw::TextLogString& text
  ) const {
    FW_ASSERT(
      (0 <= portNum) && (portNum < NUM_TEXTEVENTOUT_OUTPUT_PORTS),
      static_cast<FwAssertArgType>(portNum),
      static_cast<FwAssertArgType>(NUM_TEXTEVENTOUT_OUTPUT_PORTS)
    );
    const auto instance = this->getInstance();
    switch (instance) {
      default:
        FW_ASSERT(0, static_cast<FwAssertArgType>(instance));
        break;
    }
  }

#endif

  void CComponentBase::timeGetOut_out(
      FwIndexType portNum,
      Fw::Time& time
  ) const {
    FW_ASSERT(
      (0 <= portNum) && (portNum < NUM_TIMEGETOUT_OUTPUT_PORTS),
      static_cast<FwAssertArgType>(portNum),
      static_cast<FwAssertArgType>(NUM_TIMEGETOUT_OUTPUT_PORTS)
    );
    const auto instance = this->getInstance();
    switch (instance) {
      default:
        FW_ASSERT(0, static_cast<FwAssertArgType>(instance));
        break;
    }
  }

}

namespace M {

  bool NoEventsComponentBase::isConnected_eventOut_OutputPort(FwIndexType portNum) const {
    FW_ASSERT(
      (0 <= portNum) && (portNum < NUM_EVENTOUT_OUTPUT_PORTS),
      static_cast<FwAssertArgType>(portNum),
      static_cast<FwAssertArgType>(NUM_EVENTOUT_OUTPUT_PORTS)
    );
    bool result = false;
    const auto instance = this->getInstance();
    switch (instance) {
      default:
        FW_ASSERT(0, static_cast<FwAssertArgType>(instance));
        break;
    }
    return result;
  }

#if FW_ENABLE_TEXT_LOGGING == 1

  bool NoEventsComponentBase::isConnected_textEventOut_OutputPort(FwIndexType portNum) const {
    FW_ASSERT(
      (0 <= portNum) && (portNum < NUM_TEXTEVENTOUT_OUTPUT_PORTS),
      static_cast<FwAssertArgType>(portNum),
      static_cast<FwAssertArgType>(NUM_TEXTEVENTOUT_OUTPUT_PORTS)
    );
    bool result = false;
    const auto instance = this->getInstance();
    switch (instance) {
      default:
        FW_ASSERT(0, static_cast<FwAssertArgType>(instance));
        break;
    }
    return result;
  }

#endif

  bool NoEventsComponentBase::isConnected_timeGetOut_OutputPort(FwIndexType portNum) const {
    FW_ASSERT(
      (0 <= portNum) && (portNum < NUM_TIMEGETOUT_OUTPUT_PORTS),
      static_cast<FwAssertArgType>(portNum),
      static_cast<FwAssertArgType>(NUM_TIMEGETOUT_OUTPUT_PORTS)
    );
    bool result = false;
    const auto instance = this->getInstance();
    switch (instance) {
      default:
        FW_ASSERT(0, static_cast<FwAssertArgType>(instance));
        break;
    }
    return result;
  }

  void NoEventsComponentBase::timeGetOut_out(
      FwIndexType portNum,
      Fw::Time& time
  ) const {
    FW_ASSERT(
      (0 <= portNum) && (portNum < NUM_TIMEGETOUT_OUTPUT_PORTS),
      static_cast<FwAssertArgType>(portNum),
      static_cast<FwAssertArgType>(NUM_TIMEGETOUT_OUTPUT_PORTS)
    );
    const auto instance = this->getInstance();
    switch (instance) {
      default:
        FW_ASSERT(0, static_cast<FwAssertArgType>(instance));
        break;
    }
  }

}

#endif
