// ======================================================================
// \title  LargeSizeArrayAc.hpp
// \author Generated by fpp-to-cpp
// \brief  hpp file for LargeSize array
// ======================================================================

#ifndef LargeSizeArrayAc_HPP
#define LargeSizeArrayAc_HPP

#include <initializer_list>

#include "Fw/FPrimeBasicTypes.hpp"
#include "Fw/Types/ExternalString.hpp"
#include "Fw/Types/Serializable.hpp"
#include "Fw/Types/String.hpp"

class LargeSize :
  public Fw::Serializable
{

  public:

    // ----------------------------------------------------------------------
    // Types
    // ----------------------------------------------------------------------

    //! The element type
    using ElementType = U32;

  public:

    // ----------------------------------------------------------------------
    // Constants
    // ----------------------------------------------------------------------

    enum {
      //! The size of the array
      SIZE = 1024,
      //! The serialized size of each element
      ELEMENT_SERIALIZED_SIZE = sizeof(U32),
      //! The size of the serial representation
      SERIALIZED_SIZE = SIZE * ELEMENT_SERIALIZED_SIZE
    };

  public:

    // ----------------------------------------------------------------------
    // Constructors
    // ----------------------------------------------------------------------

    //! Constructor (default value)
    LargeSize();

    //! Constructor (primitive array)
    LargeSize(
        const ElementType (&a)[SIZE] //!< The array
    );

    //! Constructor (single element)
    explicit LargeSize(
        const ElementType& e //!< The element
    );

    //! Constructor (initializer list)
    LargeSize(
        const std::initializer_list<ElementType>& il //!< The initializer list
    );

    //! Copy constructor
    LargeSize(
        const LargeSize& obj //!< The source object
    );

  public:

    // ----------------------------------------------------------------------
    // Operators
    // ----------------------------------------------------------------------

    //! Subscript operator
    ElementType& operator[](
        const FwSizeType i //!< The subscript index
    );

    //! Const subscript operator
    const ElementType& operator[](
        const FwSizeType i //!< The subscript index
    ) const;

    //! Copy assignment operator (object)
    LargeSize& operator=(
        const LargeSize& obj //!< The source object
    );

    //! Copy assignment operator (primitive array)
    LargeSize& operator=(
        const ElementType (&a)[SIZE] //!< The source array
    );

    //! Copy assignment operator (initializer list)
    LargeSize& operator=(
        const std::initializer_list<ElementType>& il //!< The initializer list
    );

    //! Copy assignment operator (single element)
    LargeSize& operator=(
        const ElementType& e //!< The element
    );

    //! Equality operator
    bool operator==(
        const LargeSize& obj //!< The other object
    ) const;

    //! Inequality operator
    bool operator!=(
        const LargeSize& obj //!< The other object
    ) const;

#ifdef BUILD_UT

    //! Ostream operator
    friend std::ostream& operator<<(
        std::ostream& os, //!< The ostream
        const LargeSize& obj //!< The object
    );

#endif

  public:

    // ----------------------------------------------------------------------
    // Public member functions
    // ----------------------------------------------------------------------

    //! Serialization
    Fw::SerializeStatus serializeTo(
        Fw::LinearBufferBase& buffer, //!< The serial buffer
        Fw::Endianness mode = Fw::Endianness::BIG //!< Endianness of serialized buffer
    ) const;

    //! Deserialization
    Fw::SerializeStatus deserializeFrom(
        Fw::LinearBufferBase& buffer, //!< The serial buffer
        Fw::Endianness mode = Fw::Endianness::BIG //!< Endianness of serialized buffer
    );

    //! Get the dynamic serialized size of the array
    FwSizeType serializedSize() const;

#if FW_SERIALIZABLE_TO_STRING

    //! Convert array to string
    void toString(
        Fw::StringBase& sb //!< The StringBase object to hold the result
    ) const;

#endif

  private:

    // ----------------------------------------------------------------------
    // Member variables
    // ----------------------------------------------------------------------

    //! The array elements
    ElementType elements[SIZE];

};

#endif
