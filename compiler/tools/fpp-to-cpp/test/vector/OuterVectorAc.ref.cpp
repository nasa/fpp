// ======================================================================
// \title  OuterVectorAc.cpp
// \author Generated by fpp-to-cpp
// \brief  cpp file for Outer vector
// ======================================================================

#include "Fw/Types/Assert.hpp"
#include "OuterVectorAc.hpp"

// ----------------------------------------------------------------------
// Constructors
// ----------------------------------------------------------------------

Outer ::
  Outer() :
    Serializable(),
    elements(),
    length(0)
{
  *this = Outer({Inner({1, 2}), Inner({3, 4})});
}

Outer ::
  Outer(const std::initializer_list<ElementType>& il) :
    Serializable(),
    elements(),
    length(0)
{
  *this = il;
}

Outer ::
  Outer(const Outer& obj) :
    Serializable(),
    elements(),
    length(0)
{
  *this = obj;
}

// ----------------------------------------------------------------------
// Operators
// ----------------------------------------------------------------------

Outer::ElementType& Outer ::
  operator[](const FwSizeType i)
{
  FW_ASSERT(i < this->length, static_cast<FwAssertArgType>(i), static_cast<FwAssertArgType>(this->length));
  return this->elements[i];
}

const Outer::ElementType& Outer ::
  operator[](const FwSizeType i) const
{
  FW_ASSERT(i < this->length, static_cast<FwAssertArgType>(i), static_cast<FwAssertArgType>(this->length));
  return this->elements[i];
}

Outer& Outer ::
  operator=(const Outer& obj)
{
  if (this != &obj) {
    this->length = obj.length;
    for (FwSizeType index = 0; index < this->length; index++) {
      this->elements[index] = obj.elements[index];
    }
  }
  return *this;
}

Outer& Outer ::
  operator=(const std::initializer_list<ElementType>& il)
{
  // Check that the initializer does not exceed the capacity
  FW_ASSERT(il.size() <= CAPACITY, static_cast<FwAssertArgType>(il.size()), static_cast<FwAssertArgType>(CAPACITY));
  this->length = static_cast<FwSizeType>(il.size());
  FwSizeType i = 0;
  for (const auto& e : il) {
    this->elements[i] = e;
    i++;
  }
  return *this;
}

bool Outer ::
  operator==(const Outer& obj) const
{
  if (this->length != obj.length) {
    return false;
  }
  for (FwSizeType index = 0; index < this->length; index++) {
    if (!((*this)[index] == obj[index])) {
      return false;
    }
  }
  return true;
}

bool Outer ::
  operator!=(const Outer& obj) const
{
  return !(*this == obj);
}

#ifdef BUILD_UT

std::ostream& operator<<(std::ostream& os, const Outer& obj) {
  Fw::String s;
  obj.toString(s);
  os << s;
  return os;
}

#endif

// ----------------------------------------------------------------------
// Public member functions
// ----------------------------------------------------------------------

FwSizeType Outer ::
  getLength() const
{
  return this->length;
}

void Outer ::
  setLength(const FwSizeType len)
{
  FW_ASSERT(len <= CAPACITY, static_cast<FwAssertArgType>(len), static_cast<FwAssertArgType>(CAPACITY));
  this->length = len;
}

Fw::Success Outer ::
  push(const ElementType& e)
{
  if (this->length >= CAPACITY) {
    return Fw::Success::FAILURE;
  }
  this->elements[this->length] = e;
  this->length++;
  return Fw::Success::SUCCESS;
}

Fw::Success Outer ::
  pop(ElementType& e)
{
  if (this->length == 0) {
    return Fw::Success::FAILURE;
  }
  this->length--;
  e = this->elements[this->length];
  return Fw::Success::SUCCESS;
}

Fw::SerializeStatus Outer ::
  serializeTo(
      Fw::SerialBufferBase& buffer,
      Fw::Endianness mode
  ) const
{
  Fw::SerializeStatus status = Fw::FW_SERIALIZE_OK;
  // Serialize the length as a prefix
  status = buffer.serializeFrom(static_cast<FwSizeStoreType>(this->length), mode);
  if (status != Fw::FW_SERIALIZE_OK) {
    return status;
  }
  for (FwSizeType index = 0; index < this->length; index++) {
    status = buffer.serializeFrom((*this)[index], mode);
    if (status != Fw::FW_SERIALIZE_OK) {
      return status;
    }
  }
  return status;
}

Fw::SerializeStatus Outer ::
  deserializeFrom(
      Fw::SerialBufferBase& buffer,
      Fw::Endianness mode
  )
{
  Fw::SerializeStatus status = Fw::FW_SERIALIZE_OK;
  // Deserialize the length prefix
  FwSizeStoreType storedLength = 0;
  status = buffer.deserializeTo(storedLength, mode);
  if (status != Fw::FW_SERIALIZE_OK) {
    return status;
  }
  if (static_cast<FwSizeType>(storedLength) > static_cast<FwSizeType>(CAPACITY)) {
    return Fw::FW_DESERIALIZE_SIZE_MISMATCH;
  }
  this->length = static_cast<FwSizeType>(storedLength);
  for (FwSizeType index = 0; index < this->length; index++) {
    status = buffer.deserializeTo((*this)[index], mode);
    if (status != Fw::FW_SERIALIZE_OK) {
      return status;
    }
  }
  return status;
}

FwSizeType Outer ::
  serializedSize() const
{
  FwSizeType size = SIZE_PREFIX_SERIALIZED_SIZE;
  for (FwSizeType index = 0; index < this->length; index++) {
    size += this->elements[index].serializedSize();
  }
  return size;
}

#if FW_SERIALIZABLE_TO_STRING

void Outer ::
  toString(Fw::StringBase& sb) const
{
  // Clear the output string
  sb = "";

  // Vector prefix
  sb += "[ ";

  for (FwSizeType index = 0; index < this->length; index++) {
    // Vector data
    Fw::String tmp;
    this->elements[index].toString(tmp);

    if (index > 0) {
      sb += ", ";
    }
    sb += tmp;
  }

  // Vector suffix
  sb += " ]";
}

#endif
