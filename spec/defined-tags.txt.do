#!/bin/sh -e

# ----------------------------------------------------------------------
# defined-tags.txt.do
# ----------------------------------------------------------------------

redo-ifchange fpp-spec.adoc

awk '/^\[\[.*\]\]$/ {
  tag = $0
  sub(/\[\[/, "", tag)
  sub(/\]\]/, "", tag)
  print tag
}' fpp-spec.adoc | sort
