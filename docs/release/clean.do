# ----------------------------------------------------------------------
# clean.do
# ----------------------------------------------------------------------

. ./defs.sh

rm -rf fpp-spec
for file in `find . -name '*.tar' -or -name '*.zip'`
do
  rm $file
done
rm_tmp
